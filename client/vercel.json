{
  "builds": [
    {
      "src": "client/package.json",
      "use": "@vercel/node",
      "buildCommand": "npm run build",
      "outputDirectory": "client/dist"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "client/dist/$1"
    }
  ]
}
